import styled from "styled-components";

export const ContainerEventSection = styled.div`
  display: flex;
  justify-content: center;
  width: 100%;
  transform: translateY(-1100px);

  .img-bottom {
    margin-right: 2em;
    padding-top: 3.5em;
  }

  img {
    float: right;
  }

  .halo-img {
    float: left;
  }

  .box-halo {
    transform: translateX(-50px);
  }
`;
